{
  "name": "@the-list/app",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "types": "dist/app.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "dev": "node --watch --import tsx --env-file .env src/dev.ts",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prebuild": "npm run clean"
  },
  "keywords": [],
  "author": "githop",
  "license": "ISC",
  "dependencies": {
    "@hono/node-server": "^1.7.0",
    "@the-list/env": "^1.0.0",
    "hono": "^4.0.3"
  },
  "devDependencies": {
    "tsx": "^4.7.1"
  }
}
